{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ],
  "buildCommand": "cd school-notice-app && npm install && npm run build",
  "outputDirectory": "school-notice-app/dist"
}